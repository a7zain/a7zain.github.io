# Session — 2026-06-27 — Upstream page: artifacts, photos triaged, passes 1–3 shipped

## Summary

Long, productive session on the **Upstream** detail page. Designed the page around a
**satellite-lifecycle spine**, generated four real mission-analysis artifacts in Python,
fact-locked every claim against source documents Ahmed uploaded, triaged ~36 photos, and built
the page through three of four passes (skeleton, prose, interactive components). Pass 4 (images)
is the only remaining step.

## What shipped (pushed to `origin/main`)

- `5cb3d4c` — Upstream pass 1: skeleton structure
- `ebf5264` — Upstream pass 2: prose (all sections, verbatim)
- `43ea8d3` — Add mission-analysis + radiometer figures to `assets/`
- (pass 3 commit) — lifecycle ribbon + lineage timeline + competency band + affiliations
  (native responsive HTML)

Page section order: `origin → background → earned → lifecycle → artifacts → shms → research →
recognition → gallery → competency → affiliations`.

## Key decisions this session

- **Dropped the TPM framing** entirely (Ahmed's call). Through-line is the value-chain span,
  no program-manager angle.
- **Lifecycle spine** chosen as the page's organizing structure — shows command of the whole
  satellite flow while marking where Ahmed has hands-on depth vs. exposure.
- **"Show the work, not claims"** — the four mission-analysis studies become visible artifact
  cards with real computed figures, each carrying a non-obvious *finding*, not just a tool name.
- **Honesty safeguard** adopted: each artifact must survive Ahmed's one-sentence interviewer
  defense, or it's cut. Provenance always stated honestly.

## Artifacts generated (real Python computation, figures in `assets/`)

1. **Constellation revisit** (`constellation-revisit.png`) — Walker constellations over Riyadh,
   600 km / 55° / 30° off-nadir, 3-day propagation. Finding: worst-case revisit collapses
   24h→~8h from 3→6 sats then plateaus; redistributing 12 sats from 3→4 planes nearly halves
   the worst case. "Architecture beats headcount."
2. **Link budget** (`link-budget.png`) — three-band UHF/S/X budget to a Riyadh station; all
   bands close, bandwidth is the differentiator (X-band ~90 Mbps imagery pipe); ACM across a
   pass (80→133 Mbps); one station supports ~34 min imaging/day → ground-segment bound.
   "The radio was never the hard part."
3. **Trans-lunar** (`translunar.png`) — patched-conic TLI: ~3.04 km/s, ~5 days; ΔV-vs-TOF trade;
   arrive at lunar distance at 0.2 km/s while Moon moves 1.0. Rhymes with SHMS flying cislunar.
4. **O-Record** (`orecord.png`) — extracted from the radiometer capstone PDF (page 12,
   Figure 9); circular brightness-temp plot, white spots = candidate RFI.

## Fact-locks from uploaded source documents

- **SHMS** (not SHAMS): 12U space-weather CubeSat, Artemis II, 4 Apr 2026, first Arab Artemis
  mission, NIDLP, NASA designation SWC-1. Ahmed worked the **qualification campaign** (not flight
  hardware). Exobotics = UK build partner. (Confirmed via SSA infographics Ahmed uploaded.)
- **Radiometer** = CU Boulder capstone, team "The K-Bandits" (5/6/2024), CET / SpectrumX.
  Ahmed = **Systems Engineer and RAIL Lead** (owns subsystem interfaces; RAIL tracks each
  engineer's tasks per subsystem). K-band PSR, mobile platform, RFI in protected 23.6–24 GHz.
  (Confirmed via the full project report PDF.) Reframed as the *origin* of the through-line:
  passive remote sensing / EO data-integrity work before he named the direction.
- **Lineage timeline** rebuilt from the KACST "History in Space" exhibit wall (Ahmed's photo):
  2000 SaudiSat 1A/1B → 2004 SaudiSat 2 → 2007 SaudiSat 3 → 2014 SaudiSat 4 (NASA science) →
  2018 SaudiSat 5A/5B (electro-optical) → 2021 SGS1 → **2026 SHMS** (the entry Ahmed adds; the
  wall currently ends at 2021).

## Photos triaged

~36 photos reviewed across several uploads. Categorized: test facilities (TVAC chamber, anechoic
chamber, Data Physics SignalForce shaker, high-bay), clean-room selfie, KACST exhibit + building
exterior, official SSA SHMS hardware + launch + infographics, radiometer field/deployment shots,
O-Record + poster. **Decision:** Ahmed's own photos carry the page; SSA infographics used only as
*intelligence* for facts/structure (1–2 max as context), not wall art. **STAR CPU board close-up
excluded** pending mentor clearance on part-number close-ups.

## NEXT STEP (start here next session)

**Pass 4 — figures + photos + gallery.** Photos are sitting unconverted (HEIC/webp) in
`assets/Upstream/`. Two-stage Claude Code task:

- **Stage A:** convert all `assets/Upstream/` images to JPG, downscale >2400px, and **show them
  to Ahmed with filenames** (inline or as a labelled contact sheet). Then STOP and ask for the
  mapping. (Stage-A prompt was already drafted in the prior chat — rewrite it.)
- **Ahmed's only manual step:** reply with the mapping (filename → manifest name).
- **Stage B:** Claude Code renames + places into `assets/`, then wires `upstream.html` —
  figures into the three artifact cards + origin, SHMS hero + facility group, background
  KACST photo, and the gallery — leaving "image pending: `<name>`" for any missing manifest name.

Manifest: `cleanroom.jpg`, `thermal-vacuum.jpg`, `vibration-stand.jpg`, `anechoic-chamber.jpg`,
`integration-highbay.jpg`, `kacst-exterior.jpg`, `kacst-history-wall.jpg`, `shms-satellite.jpg`,
`sls-launch.jpg`, `radiometer-field.jpg`. Exclude the STAR CPU board.

After Pass 4: full read-through of the finished Upstream page, then move to **Academia** or
**Downstream**.

## Reminders / gotchas

- Strategy Claude **cannot** access Ahmed's Mac or move local files — only the sandboxed Linux
  container (hands generated files to Ahmed to download). File ops on the Mac are Claude Code's
  job; Ahmed only supplies the photo mapping.
- Claude Code cannot reliably identify which photo is which — Ahmed must map them.
- Push is Ahmed's job (HTTPS push from Claude Code is credential-blocked).
- Every Claude Code prompt opens with the worktree guardrail.
